package com.example.mypractica2.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de colores principal (modo claro)
val PrimaryBlue = Color(0xFF0D47A1) // Un azul más oscuro y elegante
val SecondaryAqua = Color(0xFF00796B) // Un verde azulado como secundario
val BackgroundStart = Color(0xFFE3F2FD) // Inicio del gradiente (casi blanco)
val BackgroundEnd = Color(0xFFB3E5FC) // Fin del gradiente (azul claro)

// Paleta de colores para el modo oscuro
val DarkPrimary = Color(0xFF42A5F5)
val DarkSecondary = Color(0xFF4DB6AC)
val DarkBackgroundStart = Color(0xFF011E3D)
val DarkBackgroundEnd = Color(0xFF003756)